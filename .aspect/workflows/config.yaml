# See https://docs.aspect.build/workflows/configuration/tasks
workspaces:
  .:
    icon: deer
    tasks:
      - gazelle:
          without: true
  common:
    tasks:
      - buildifier:
          without: true
  language/js:
    tasks:
      - buildifier:
          without: true
  language/kotlin:
    tasks:
      - buildifier:
          without: true
  language/orion:
    tasks:
      - buildifier:
          without: true
  runner:
    tasks:
      - buildifier:
          without: true
  runner/e2e/bin:
    tasks:
      - gazelle:
          without: true
      - buildifier:
          without: true
tasks:
  - buildifier:
  - gazelle:
  - test:
notifications:
  github: {}
