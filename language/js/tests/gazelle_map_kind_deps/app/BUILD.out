load("//tools:kinds.bzl", "my_ts_project")

# Generate a ts_project for main.ts.
# gazelle:js_files **/*.{js,ts}

# BUG: main.ts imports //:provider but app gets no deps.

my_ts_project(
    name = "app",
    srcs = ["main.ts"],
    deps = ["//:provider"],
)
