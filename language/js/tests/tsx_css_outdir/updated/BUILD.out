load("@aspect_rules_ts//ts:defs.bzl", "ts_project")

ts_project(
    name = "updated",
    srcs = ["lib.tsx"],
    assets = [
        "unused.css",  # keep
        "lib.css",
    ],
    out_dir = "dist",
    tsconfig = "//:tsconfig",
)
