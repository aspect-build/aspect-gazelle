/bazel-*
/*/bazel-*
/*/e2e/*/bazel-*
/language/*/bazel-*
/language/*/e2e/*/bazel-*
**/node_modules

# prebuilt's MODULE.bazel is a template (version/binaries stamped at release),
# so its lockfile is not committed like the other workspaces'.
/prebuilt/MODULE.bazel.lock
