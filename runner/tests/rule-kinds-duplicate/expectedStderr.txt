aspect-gazelle: ERROR: orion plugin "tests/rule-kinds-duplicate/override.axl" registered rule kind "ts_project" (From="@i_replaced_aspect_rules_ts//:defs.bzl") which is already provided by the "js" gazelle language. Remove the aspect.gazelle_rule_kind("ts_project") call from "tests/rule-kinds-duplicate/override.axl" — the "js" language's rule resolution for "ts_project" is otherwise silently broken.
