diff --git a/v2/walk/walk.go b/v2/walk/walk.go
index 8c3c347..fcb615c 100644
--- a/v2/walk/walk.go
+++ b/v2/walk/walk.go
@@ -386,6 +386,12 @@ func newWalker(c *config.Config, cexts []config.Configurer, dirs []string, mode
 	}
 	// END-PATCH(gitignore) ---
 
+	// PATCH(watch) ---
+	if hook, ok := c.Exts["aspect:walkCache:load"].(func(any)); ok {
+		hook(&w.cache.entryMap)
+	}
+	// END-PATCH(watch) ---
+
 	// Asynchronously populate the walker cache in the background.
 	go w.populateCache(mode)
 
