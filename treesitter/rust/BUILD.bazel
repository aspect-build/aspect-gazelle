load("@rules_go//go:def.bzl", "go_library")

go_library(
    name = "rust",
    srcs = [
        "binding.go",
        "@tree-sitter-rust//:srcs",
    ],
    cgo = True,
    copts = ["-Iexternal/*tree-sitter-rust"],
    importpath = "github.com/aspect-build/aspect-gazelle/treesitter/rust",
    visibility = ["//visibility:public"],
)
