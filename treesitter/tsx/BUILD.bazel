load("@rules_go//go:def.bzl", "go_library")

go_library(
    name = "tsx",
    srcs = [
        "binding.go",
        "@tree-sitter-typescript//:tsx-srcs",
    ],
    cgo = True,
    copts = ["-Iexternal/*tree-sitter-typescript"],
    importpath = "github.com/aspect-build/aspect-gazelle/treesitter/tsx",
    visibility = ["//visibility:public"],
)
