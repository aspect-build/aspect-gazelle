load("@rules_go//go:def.bzl", "go_library")

go_library(
    name = "ruby",
    srcs = [
        "binding.go",
        "@tree-sitter-ruby//:srcs",
    ],
    cgo = True,
    copts = ["-Iexternal/*tree-sitter-ruby"],
    importpath = "github.com/aspect-build/aspect-gazelle/treesitter/ruby",
    visibility = ["//visibility:public"],
)
