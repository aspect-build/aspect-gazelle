load("@rules_go//go:def.bzl", "go_library")

go_library(
    name = "starlark",
    srcs = [
        "binding.go",
        "@tree-sitter-starlark//:srcs",
    ],
    cgo = True,
    copts = ["-Iexternal/*tree-sitter-starlark"],
    importpath = "github.com/aspect-build/aspect-gazelle/treesitter/starlark",
    visibility = ["//visibility:public"],
)
